a + b - c * d / e % f ** g
a == b != c <= d >= e < f > g
a && b || c & d | e ^ ~f !g
a = b += c -= d <<= e >>= f |= g &= h
a -> b; a := b; a :/ b; a..b; a...b; a::b
x = y ? 1 : -2
d dist { 0 := 1, [1..3] :/ 6 }
t... args
#1
