/*
 * Generated by peakrdl-pss <version> -- do not edit.
 *
 * SystemRDL register model exported as a PSS register package.
 * Top: deep
 */
package deep {
    import addr_reg_pkg::*;
    import std_pkg::*;

    // ---- register types ----

    // deep.blk0.m0.l0.a (+53 more)
    struct deep__leaf_t__r_t_s : packed_s<LITTLE_ENDIAN> {
        bit[32] f; // [31:0] sw=rw hw=r reset=0x0
    }
    pure component deep__leaf_t__r_t_c : reg_c<deep__leaf_t__r_t_s, READWRITE, 32> {}

    // ---- register groups ----

    // deep.blk0.m0.l0 (+17 more)
    pure component deep__leaf_t_c : reg_group_c {
        deep__leaf_t__r_t_c a;
        deep__leaf_t__r_t_c b;
        deep__leaf_t__r_t_c c[4];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["a"]: return 0x0;
                ["b"]: return 0x4;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["c"]: return 0x8 + index*0x4;
                default: return -1;
            }
        }
    }

    // deep.blk0.m0 (+5 more)
    pure component deep__mid_t_c : reg_group_c {
        deep__leaf_t_c l0;
        deep__leaf_t_c l1;
        deep__leaf_t_c l2[3];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["l0"]: return 0x0;
                ["l1"]: return 0x20;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["l2"]: return 0x40 + index*0x20;
                default: return -1;
            }
        }
    }

    // deep.blk0 (+2 more)
    pure component deep__block_t_c : reg_group_c {
        deep__mid_t_c m0;
        deep__mid_t_c m1[2];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["m0"]: return 0x0;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["m1"]: return 0x200 + index*0x100;
                default: return -1;
            }
        }
    }

    // deep
    pure component deep_c : reg_group_c {
        deep__block_t_c blk0;
        deep__block_t_c blk1;
        deep__block_t_c blks[4];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["blk0"]: return 0x0;
                ["blk1"]: return 0x1000;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["blks"]: return 0x4000 + index*0x1000;
                default: return -1;
            }
        }
    }
}
