/*
 * Generated by peakrdl-pss <version> -- do not edit.
 *
 * SystemRDL register model exported as a PSS register package.
 * Top: encode_regs
 */
package encode {
    import addr_reg_pkg::*;
    import std_pkg::*;

    // ---- encodings ----

    // encoding mode_e
    static const bit[2] mode_e__off = 0x0;
    static const bit[2] mode_e__slow = 0x1;
    static const bit[2] mode_e__fast = 0x2;

    // ---- register types ----

    // encode_regs.cfg
    struct encode_regs__cfg_s : packed_s<LITTLE_ENDIAN> {
        bit[2] mode; // [1:0] sw=rw hw=r reset=0x0
        bit[2] backup_mode; // [3:2] sw=rw hw=r reset=0x0
        bit[4] plain; // [7:4] sw=rw hw=r
    }
    pure component encode_regs__cfg_c : reg_c<encode_regs__cfg_s, READWRITE, 32> {}

    // ---- register groups ----

    // encode_regs
    pure component encode_regs_c : reg_group_c {
        encode_regs__cfg_c cfg;

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["cfg"]: return 0x0;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                default: return -1;
            }
        }
    }
}
