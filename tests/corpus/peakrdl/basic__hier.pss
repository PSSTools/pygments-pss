/*
 * Generated by peakrdl-pss <version> -- do not edit.
 *
 * SystemRDL register model exported as a PSS register package.
 * Top: basic
 */
package basic {
    import addr_reg_pkg::*;
    import std_pkg::*;

    // ---- register types ----

    // basic.foo.r1
    struct basic__foo__r1_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f1; // [15:0] sw=rw hw=r reset=0x4d2
        bit f2; // [16] sw=rw hw=r reset=0x0
        bit f3; // [17] sw=rw hw=r reset=0x0
        bit[10] rsvd_18; // [27:18] reserved
        bit[2] f5; // [29:28] sw=rw hw=r reset=0x0
        bit[2] f4; // [31:30] sw=rw hw=r reset=0x0
    }
    pure component basic__foo__r1_c : reg_c<basic__foo__r1_s, READWRITE, 32> {}

    // basic.foo.r2
    struct basic__foo__r2_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f1; // [15:0] sw=rw hw=r reset=0x4d2
        bit f2; // [16] sw=rw hw=r reset=0x0
        bit f3; // [17] sw=rw hw=r reset=0x0
        bit[10] rsvd_18; // [27:18] reserved
        bit[2] f5; // [29:28] sw=rw hw=r reset=0x0
        bit[2] f4; // [31:30] sw=rw hw=r reset=0x0
    }
    pure component basic__foo__r2_c : reg_c<basic__foo__r2_s, READWRITE, 32> {}

    // basic.foo.r3
    struct basic__foo__r3_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f1; // [15:0] sw=w hw=r reset=0xc8
        bit f2; // [16] sw=rw hw=r reset=0x0
        bit f3; // [17] sw=rw hw=r reset=0x0
        bit[10] rsvd_18; // [27:18] reserved
        bit[2] f5; // [29:28] sw=rw hw=r reset=0x0
        bit[2] f4; // [31:30] sw=rw hw=r reset=0x0
    }
    pure component basic__foo__r3_c : reg_c<basic__foo__r3_s, READWRITE, 32> {}

    // basic.foo.r4
    struct basic__foo__r4_s : packed_s<LITTLE_ENDIAN> {
        bit f; // [0] sw=rw hw=r
    }
    pure component basic__foo__r4_c : reg_c<basic__foo__r4_s, READWRITE, 32> {}

    // basic.bar[][].r1[]
    struct basic__bar__r1_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f; // [15:0] sw=rw hw=r reset=0x4d2
    }
    pure component basic__bar__r1_c : reg_c<basic__bar__r1_s, READWRITE, 32> {}

    // basic.bar2.r1[]
    struct basic__bar2__r1_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f; // [15:0] sw=w hw=r reset=0xc8
    }
    pure component basic__bar2__r1_c : reg_c<basic__bar2__r1_s, WRITEONLY, 32> {}

    // ---- register groups ----

    // basic.foo
    pure component basic__foo_c : reg_group_c {
        basic__foo__r1_c r1;
        basic__foo__r2_c r2;
        basic__foo__r3_c r3;
        basic__foo__r4_c r4;

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["r1"]: return 0x0;
                ["r2"]: return 0x4;
                ["r3"]: return 0x8;
                ["r4"]: return 0xc;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                default: return -1;
            }
        }
    }

    // basic.bar[][]
    pure component basic__bar_c : reg_group_c {
        basic__bar__r1_c r1[4];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["r1"]: return 0x0 + index*0x4;
                default: return -1;
            }
        }
    }

    // basic.bar2
    pure component basic__bar2_c : reg_group_c {
        basic__bar2__r1_c r1[4];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["r1"]: return 0x0 + index*0x4;
                default: return -1;
            }
        }
    }

    // basic
    pure component basic_c : reg_group_c {
        basic__foo_c foo;
        basic__bar_c bar[12]; // bar[4][3] flattened row-major
        basic__bar2_c bar2;

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["foo"]: return 0x0;
                ["bar2"]: return 0x8000;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["bar"]: return 0x1000 + index*0x100;
                default: return -1;
            }
        }
    }
}
