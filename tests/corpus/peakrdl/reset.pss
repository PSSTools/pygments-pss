/*
 * Generated by peakrdl-pss <version> -- do not edit.
 *
 * SystemRDL register model exported as a PSS register package.
 * Top: reset
 */
package reset {
    import addr_reg_pkg::*;
    import std_pkg::*;

    // ---- register types ----

    // reset.ctrl (+2 more)
    struct reset__r_t_s : packed_s<LITTLE_ENDIAN> {
        bit[16] f; // [15:0] sw=rw hw=r reset=0x1234
        bit[16] g; // [31:16] sw=rw hw=r reset=0x5678
    }
    pure component reset__r_t_c : reg_c<reset__r_t_s, READWRITE, 32> {}

    // reset.status
    struct reset__no_reset_t_s : packed_s<LITTLE_ENDIAN> {
        bit[32] f; // [31:0] sw=rw hw=r
    }
    pure component reset__no_reset_t_c : reg_c<reset__no_reset_t_s, READWRITE, 32> {}

    // ---- register groups ----

    // reset
    pure component reset_c : reg_group_c {
        reset__r_t_c ctrl;
        reset__r_t_c ctrl_reset;
        reset__no_reset_t_c status;
        reset__r_t_c bank[4];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                ["ctrl"]: return 0x0;
                ["ctrl_reset"]: return 0x4;
                ["status"]: return 0x8;
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["bank"]: return 0x10 + index*0x4;
                default: return -1;
            }
        }
    }
}
