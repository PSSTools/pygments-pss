/*
 * Generated by peakrdl-pss <version> -- do not edit.
 *
 * SystemRDL register model exported as a PSS register package.
 * Top: wide
 */
package wide {
    import addr_reg_pkg::*;
    import std_pkg::*;

    // ---- register types ----

    // wide.blk[].reg0[]
    struct wide__blk_t__r_t_s : packed_s<LITTLE_ENDIAN> {
        bit[32] f; // [31:0] sw=rw hw=r reset=0x0
    }
    pure component wide__blk_t__r_t_c : reg_c<wide__blk_t__r_t_s, READWRITE, 32> {}

    // ---- register groups ----

    // wide.blk[]
    pure component wide__blk_t_c : reg_group_c {
        wide__blk_t__r_t_c reg0[64];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["reg0"]: return 0x0 + index*0x4;
                default: return -1;
            }
        }
    }

    // wide
    pure component wide_c : reg_group_c {
        wide__blk_t_c blk[64];

        pure function bit[64] get_offset_of_instance(string name) {
            match (name) {
                default: return -1;
            }
        }

        pure function bit[64] get_offset_of_instance_array(string name, int index) {
            match (name) {
                ["blk"]: return 0x0 + index*0x100;
                default: return -1;
            }
        }
    }
}
